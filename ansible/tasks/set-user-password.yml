---
# Rotate the user password. Called from maintenance.yml when
# user_password is defined in secrets.yml.

- name: Set user password.
  ansible.builtin.user:
    name: "{{ ansible_user }}"
    password: "{{ user_password | password_hash('sha512') }}"
