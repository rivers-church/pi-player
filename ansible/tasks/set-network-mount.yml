---
# Update or add an SMB network mount. Called from maintenance.yml when
# mount_what is defined in secrets.yml.
#
# Also updates /etc/ansible/facts.d/pi_player.fact so the pull playbook
# reflects the new state on subsequent runs.

- name: Resolve target user's uid/gid for mount ownership.
  ansible.builtin.getent:
    database: passwd
    key: "{{ ansible_user }}"

- name: Set uid/gid facts for the mount template.
  ansible.builtin.set_fact:
    _mount_uid: "{{ ansible_facts.getent_passwd[ansible_user][1] }}"
    _mount_gid: "{{ ansible_facts.getent_passwd[ansible_user][2] }}"

- name: Update device-local facts (keeps pull playbook in sync).
  ansible.builtin.copy:
    dest: /etc/ansible/facts.d/pi_player.fact
    mode: '0644'
    content: |
      {
        "mount_enabled": true,
        "mount_what": "{{ mount_what }}",
        "mount_where": "{{ mount_where }}",
        "mount_user": "{{ mount_user }}",
        "mount_domain": "{{ mount_domain | default('') }}"
      }

- name: Write CIFS credentials file.
  ansible.builtin.copy:
    dest: /etc/samba/credentials
    mode: '0600'
    content: |
      username={{ mount_user }}
      password={{ mount_password }}
      {% if mount_domain | default('') %}domain={{ mount_domain }}{% endif %}

- name: Derive the systemd unit names from the mount point.
  ansible.builtin.command:
    cmd: "systemd-escape --path --suffix={{ item }} {{ mount_where }}"
  register: unit_names
  changed_when: false
  loop:
    - mount
    - automount

- name: Set unit name facts.
  ansible.builtin.set_fact:
    mount_unit: "{{ unit_names.results[0].stdout }}"
    automount_unit: "{{ unit_names.results[1].stdout }}"
    dns_wait_unit: "smb-dns-wait-{{ mount_what | regex_replace('^//([^/]+)/.*$', '\\1') }}.service"

- name: Create the mount point directory.
  ansible.builtin.file:
    path: "{{ mount_where }}"
    state: directory
    mode: '0755'

- name: Template the DNS-wait service unit.
  ansible.builtin.template:
    src: "{{ playbook_dir }}/templates/networkshare.dns-wait.service.j2"
    dest: "/etc/systemd/system/{{ dns_wait_unit }}"
    mode: '0644'

- name: Template the systemd mount unit.
  ansible.builtin.template:
    src: "{{ playbook_dir }}/templates/networkshare.mount.j2"
    dest: "/etc/systemd/system/{{ mount_unit }}"
    mode: '0644'
  register: mount_unit_file

- name: Template the systemd automount unit.
  ansible.builtin.template:
    src: "{{ playbook_dir }}/templates/networkshare.automount.j2"
    dest: "/etc/systemd/system/{{ automount_unit }}"
    mode: '0644'
  register: automount_unit_file

- name: Disable direct mount unit (managed by automount).
  ansible.builtin.systemd_service:
    name: "{{ mount_unit }}"
    enabled: false
    daemon_reload: true
  failed_when: false

- name: Check if mount point is currently active.
  ansible.builtin.command:
    cmd: mountpoint -q {{ mount_where }}
  register: mountpoint_check
  changed_when: false
  failed_when: false

- name: Enable the automount unit.
  ansible.builtin.systemd_service:
    name: "{{ automount_unit }}"
    enabled: true
    daemon_reload: true

- name: Start or restart the automount unit (skipped if already mounted).
  ansible.builtin.systemd_service:
    name: "{{ automount_unit }}"
    state: "{{ 'restarted' if automount_unit_file.changed or mount_unit_file.changed else 'started' }}"
  when: mountpoint_check.rc != 0
